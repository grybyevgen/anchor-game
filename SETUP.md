# 📋 Инструкция по настройке проекта

## Шаг 1: Создание проекта Supabase

1. Перейдите на [https://supabase.com](https://supabase.com)
2. Зарегистрируйтесь или войдите в аккаунт
3. Нажмите **"New Project"**
4. Заполните форму:
   - **Name**: anchor (или любое другое имя)
   - **Database Password**: создайте надежный пароль (сохраните его!)
   - **Region**: выберите ближайший регион
5. Нажмите **"Create new project"**
6. Дождитесь создания проекта (обычно 1-2 минуты)

## Шаг 2: Получение ключей API

1. В вашем проекте Supabase перейдите в **Settings** (шестеренка внизу слева)
2. Выберите **API**
3. Скопируйте следующие значения:
   - **Project URL** (например: `https://abcdefghijklmnop.supabase.co`)
   - **anon public** key (длинная строка)
   - **service_role** key (длинная строка, **НЕ ПУБЛИКУЙТЕ ЕГО!**)

## Шаг 3: Создание базы данных

1. В Supabase Dashboard перейдите в **SQL Editor** (иконка базы данных слева)
2. Нажмите **"New query"**
3. Откройте файл `database/schema.sql` из проекта
4. Скопируйте весь SQL код
5. Вставьте в SQL Editor в Supabase
6. Нажмите **"Run"** (или Ctrl+Enter)
7. Должно появиться сообщение об успешном выполнении

## Шаг 4: Настройка Backend

1. Перейдите в папку `backend`:
   ```bash
   cd backend
   ```

2. Скопируйте файл `.env.example` в `.env`:
   ```bash
   cp .env.example .env
   ```
   
   На Windows:
   ```cmd
   copy .env.example .env
   ```

3. Откройте файл `.env` и заполните его:
   ```env
   SUPABASE_URL=https://ваш-проект.supabase.co
   SUPABASE_ANON_KEY=ваш-anon-key
   SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-key
   PORT=3000
   ```

4. Установите зависимости:
   ```bash
   npm install
   ```

5. Запустите сервер:
   ```bash
   npm start
   ```

   Вы должны увидеть:
   ```
   ✅ Supabase подключен успешно
   📦 URL: https://...
   ✅ Подключение к базе данных успешно
   🚢 Сервер запущен на порту 3000
   ```

## Шаг 5: Настройка Frontend

1. Откройте файл `frontend/app.js`
2. Найдите строку:
   ```javascript
   const API_URL = 'http://localhost:3000/api';
   ```
3. Если ваш backend размещен на другом сервере, измените URL:
   ```javascript
   const API_URL = 'https://your-backend-url.com/api';
   ```

## Шаг 6: Размещение Frontend

### Вариант A: GitHub Pages

1. Создайте репозиторий на GitHub
2. Загрузите папку `frontend/` в репозиторий
3. В настройках репозитория включите GitHub Pages
4. Скопируйте URL (например: `https://username.github.io/repo-name/`)

### Вариант B: Vercel

1. Установите Vercel CLI: `npm i -g vercel`
2. Перейдите в папку `frontend`
3. Выполните: `vercel`
4. Следуйте инструкциям

### Вариант C: Netlify

1. Перейдите на [netlify.com](https://netlify.com)
2. Перетащите папку `frontend/` в Netlify
3. Получите URL вашего сайта

## Шаг 7: Настройка Telegram Bot

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота
5. Отправьте команду `/newapp`
6. Выберите вашего бота
7. Заполните форму:
   - **Title**: Морское Судоходство
   - **Description**: Игра про судоходство
   - **Photo**: загрузите изображение (опционально)
   - **Web App URL**: вставьте URL вашего frontend (из шага 6)
8. Готово! Теперь ваш бот имеет кнопку для запуска Web App

## Проверка работы

1. Запустите backend сервер
2. Откройте вашего бота в Telegram
3. Нажмите на кнопку Web App
4. Должно открыться приложение
5. Попробуйте купить судно и отправить его в порт

## Возможные проблемы

### Ошибка подключения к Supabase
- Проверьте правильность URL и ключей в `.env`
- Убедитесь, что проект Supabase активен

### Ошибка при выполнении SQL
- Убедитесь, что скопировали весь SQL код
- Проверьте, что расширение `uuid-ossp` включено

### CORS ошибки
- Убедитесь, что backend запущен
- Проверьте URL в `frontend/app.js`

### Судна не загружаются
- Проверьте консоль браузера на ошибки
- Убедитесь, что пользователь инициализирован через `/api/users/init`

## Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи backend сервера
2. Проверьте консоль браузера (F12)
3. Проверьте Supabase Dashboard → Logs
