-- Удаление порта Мурманск и всех связанных данных
-- ВНИМАНИЕ: Этот скрипт удалит порт Мурманск и все связанные с ним данные
-- Выполните этот скрипт, если порт Мурманск уже существует в базе данных

-- Удаляем грузы порта Мурманск
DELETE FROM port_cargo 
WHERE port_id IN (SELECT id FROM ports WHERE name = 'Порт Мурманск');

-- Удаляем грузы на рынке, связанные с портом Мурманск
DELETE FROM market_cargo 
WHERE port_id IN (SELECT id FROM ports WHERE name = 'Порт Мурманск');

-- Удаляем сам порт Мурманск
-- ВНИМАНИЕ: Если есть суда, которые находятся в порту Мурманск или направляются туда,
-- это может вызвать проблемы. Рекомендуется сначала переместить все суда в другой порт.
DELETE FROM ports WHERE name = 'Порт Мурманск';
