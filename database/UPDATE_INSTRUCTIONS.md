# Инструкция по обновлению базы данных

## Порядок выполнения:

### 1. Выполнить вайп базы данных

Откройте файл `database/wipe_database.sql` в Supabase SQL Editor и выполните его.

**ВНИМАНИЕ:** Это удалит ВСЕ данные из базы (пользователи, суда, грузы, порты и т.д.)

### 2. Загрузить обновленную схему

Откройте файл `database/schema.sql` в Supabase SQL Editor и выполните его.

Это создаст:
- ✅ Все таблицы с актуальными значениями по умолчанию:
  - `users.coins` = 2000 (новые пользователи)
  - `ships.fuel` = 200 (новые суда)
  - `ships.max_fuel` = 200 (новые суда)
- ✅ Все функции (add_user_coins, spend_user_coins, calculate_port_cargo_price)
- ✅ Начальные порты (СПб, Владивосток, Новороссийск)
- ✅ Начальные грузы в портах с динамическим ценообразованием

## Проверка после обновления

После выполнения обоих скриптов проверьте:

1. Таблицы созданы:
   ```sql
   SELECT table_name FROM information_schema.tables 
   WHERE table_schema = 'public' 
   ORDER BY table_name;
   ```

2. Значения по умолчанию:
   ```sql
   -- Проверка DEFAULT для users
   SELECT column_name, column_default 
   FROM information_schema.columns 
   WHERE table_name = 'users' AND column_name = 'coins';
   -- Должно быть: 2000

   -- Проверка DEFAULT для ships
   SELECT column_name, column_default 
   FROM information_schema.columns 
   WHERE table_name = 'ships' 
   AND column_name IN ('fuel', 'max_fuel');
   -- Должно быть: 200
   ```

3. Порты созданы:
   ```sql
   SELECT name FROM ports;
   -- Должно быть 3 порта
   ```

4. Функции работают:
   ```sql
   SELECT routine_name 
   FROM information_schema.routines 
   WHERE routine_schema = 'public';
   -- Должно быть 3 функции
   ```

## Готово! 🎉

После выполнения этих шагов база данных будет полностью обновлена и готова к использованию.
