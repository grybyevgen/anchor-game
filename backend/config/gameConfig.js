// Игровые константы и конфигурация

module.exports = {
    // Цены судов (Хардкорная модель: дороже вход в игру)
    shipPrices: {
        tanker: 2000,   // было 1000
        cargo: 3000,    // было 1500
        supply: 2500    // было 1200
    },

    // Названия судов
    shipNames: {
        tanker: 'Танкер',
        cargo: 'Грузовое судно',
        supply: 'Снабженец'
    },

    // Время путешествия (в миллисекундах)
    travelTime: {
        default: 30000, // 30 секунд для тестирования
        // В продакшене можно использовать:
        // short: 5 * 60 * 1000,  // 5 минут
        // medium: 15 * 60 * 1000, // 15 минут
        // long: 30 * 60 * 1000    // 30 минут
    },

    // Расход топлива (теперь зависит от расстояния)
    // Хардкор: расход заметный, но позволяет сделать полный круг
    fuelCost: {
        maxFuel: 400,   // Бак оставляем тем же, чтобы не ломать баланс
        // Расход топлива на морскую милю по типам судов (подправлен, чтобы один полный круг был возможен)
        consumptionPerMile: {
            // Для стартового танкера: ~0.025 ед. топлива за милю
            // При расстоянии порядка 12 000+ миль за полный круг
            // общий расход получается ≈ 300–340 топлива, что укладывается в бак 400
            tanker: 0.025,
            cargo: 0.022,
            supply: 0.02
        },
        // Минимальный расход топлива (даже на короткие расстояния)
        minFuelPerTravel: 3  // немного смягчим, чтобы короткие переходы не были слишком дорогими
    },

    // Скорость судов (морские мили в час)
    shipSpeed: {
        tanker: 15,    // Морские мили в час
        cargo: 18,
        supply: 20
    },

    // Экономика
    economy: {
        // Базовая стоимость груза по типам (логическая ценность; хардкор — понижена, чтобы резать маржу)
        cargoBaseValue: {
            oil: 40,        // было 50
            materials: 20,  // было 25
            provisions: 12  // было 15
        },
        
        // Множитель цены продажи в зависимости от расстояния (монет за морскую милю за единицу груза)
        // Вариант A: сильно режем бонус за расстояние, чтобы один рейс не давал x3 к капиталу
        distancePriceMultiplier: 0.005, // было 0.05, теперь бонус в 10 раз слабее
        
        // Множитель спроса/предложения в порту (-50% до +100%)
        demandMultiplier: {
            min: 0.5,   // Много груза - цена ниже
            max: 2.0    // Мало груза - цена выше
        },
        
        // Бонус от уровня экипажа (старая система, но теперь используется по-другому)
        rewardMultiplierPerCrewLevel: 0.02, // +2% за уровень (было 10%)
        
        // Множитель цены на рынке (от продажной цены)
        marketPriceMultiplier: 0.8, // 80% от продажной цены
        
        // Портовые сборы - полностью процентные от прибыли
        // Хардкор: выше доля портовых сборов
        portFees: {
            unloadingPercentage: 0.25  // было 0.15
        },
        
        // Налог на прибыль от доставки (для борьбы с инфляцией)
        // Хардкор: увеличенный налог на прибыль
        profitTax: 0.20,              // было 0.15
        
        // Ежедневные операционные расходы
        // Хардкор: флот содержать дорого, заставляет считать деньги
        dailyOperatingCost: {
            base: 100,                 // было 50
            perCrewLevel: 20,          // было 10
            perShip: 50,               // было 20
            fuelPrice: 2               // было 1
        },
        
        repairCostPerHealth: 5,
        
        // Динамическое ценообразование в портах (для покупки груза)
        portCargoPricing: {
            basePrice: 5,           // Базовая цена (средняя)
            minPrice: 2,             // Минимальная цена (когда груза много)
            maxPrice: 15,            // Максимальная цена (когда груза мало)
            referenceAmount: 5000,   // Эталонное количество для расчета
            minAmount: 100           // Минимум, ниже которого цена = maxPrice
        }
    },

    // Начальные значения
    initial: {
        userCoins: 2000,
        shipFuel: 400,      // Полный бак при создании - для возможности дальних рейсов
        shipMaxFuel: 400,   // Увеличено до 400 для дальних рейсов (Владивосток-Новороссийск требует ~300)
        shipHealth: 100,
        shipMaxHealth: 100,
        shipCrewLevel: 1
    },

    // Валидация
    validation: {
        minCargoAmount: 1,
        maxCargoAmount: 100, // Максимальное количество груза на судне - 100 единиц
        minPrice: 0,
        maxPrice: 1000000,
        minCrewLevel: 1,
        maxCrewLevel: 10
    }
};
